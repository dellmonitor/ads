#include <stdio.h>
#include <stdlib.h>

struct PriorityQueue {
	int *heap;
	int cap, count;
};

int main(int argc, char **argv)
{
	int k, i, n;
	scanf("%d", &k);
	sizes[k];
	for (i = 0; i < k; i++) {
		scanf("%d", sizes[i]);
		n = n + i;
	}
	struct PriorityQueue q = InitPriorityQueue(n);

	return 0;
}
